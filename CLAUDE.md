# הקשר הפרויקט — נטען אוטומטית בכל סשן

קורא את זה בתחילת כל סשן בריפו הזה. מטרתו: שסשן חדש, ממחשב אחר, יידע תוך
שנייה איפה הכל עומד בלי שאילייה יסביר מחדש.

## מה זה הריפו

מכונת ייצור ספרים ל-Amazon KDP, וספר ראשון בתוכה.

| נתיב | מה |
|---|---|
| `.claude/skills/kdp-factory/` | הידע: חוקי KDP מאומתים, פרומפט 8 תפקידים, מחשבון תמלוגים |
| `kdp/book_01/` | ספר 1 — Color, Cut & Glue: Animal Homes |
| `kdp/book_01/output/` | ה-PDF-ים הבנויים |
| `skills-backup/` | גיבוי הסקילים האישיים של אילייה (BubiPop) |
| `backup.sh` | מסנכרן סקילים, מוסיף הכל, דוחף לענף ול-main |

## החלטות נעולות — אל תפתח מחדש בלי סיבה

| החלטה | המספר שמאחוריה |
|---|---|
| **110 עמודים** | KDP גובה **$2.84 קבוע** להדפסת ש/ל בטרים גדול מ-24 עד 110 עמ'. מעל זה — חיוב לעמוד. 110 = מקסימום ערך בעלות מינימלית |
| **8.5×11, שחור-לבן** | צבע Premium יעלה את ההדפסה ל-$9.80 ויהפוך רווח להפסד |
| **מחיר $9.99** | מתחת לזה התמלוג צונח מ-60% ל-50%. רווח: **$3.15** ליחידה |
| **כריכה צבעונית** | KDP לא גובה תוספת על כריכה. שם כל הצבע |
| **הדפסה חד-צדדית** | "no bleed-through" היא נקודת המכירה מס' 1 בביקורות, וגם תנאי לגזירה |
| **גיל 3–6** | לא 2–4. בן 2 לא גוזר במספריים |
| **Bubi לא בכותרת** | ערוץ BubiPop Kids היה 3 מנויים ב-09/2026. אף הורה לא מחפש "Bubi" |

## כללים שאסור לשבור

1. **אל תמציא נתוני שוק.** BSR, ביקורות, מחירי מתחרים — או שחיפשת, או שכתוב `UNVERIFIED` עם אחוז ודאות.
2. **כל חישוב כסף מוצג כנוסחה עם המספרים בפנים**, לא רק תוצאה.
3. **אל תייעץ להצהיר חסר על תוכן AI** בטופס KDP. האיורים נוצרו ב-AI — מצהירים YES.
4. **אפס שמות/דמויות מוגנים בזכויות יוצרים.** זה סגירת חשבון, לא הערת סגנון.
5. **דבר עברית עם אילייה.** תוכן הספר באנגלית (שוק ארה"ב).

## המצב כרגע (עדכן את השורה הזו בכל שינוי משמעותי)

**2026-09-23 — הספר בנוי, מעוצב ומאומת. חסרים 36 מתוך 46 האיורים.**

```bash
python3 kdp/book_01/check_art.py --dir kdp/book_01/art   # מה חסר
```

הצינור, אחרי שמוסיפים איורים ל-`kdp/book_01/raw/`:
```bash
cd kdp/book_01
python3 prep_art.py --in raw --out art
python3 check_art.py --dir art
python3 build_interior.py --art ./art --out output/interior.pdf
COVER_THEME=sunshine python3 build_cover.py --art ./art --out output/cover.pdf
```

תלויות: `pip install reportlab pillow scipy pymupdf`

## גיבוי

Stop hook דוחף אוטומטית בסוף כל תשובה, לענף ול-`main`.
שורת הסטטוס מראה 🟣 כשהכל מגובה. ידנית: `./backup.sh`.
